changeset = '{lrev}:{lhash}:{phase} \n {ldesc} {ltag} \n {lage} {luser} \n\n'

lrev = "{label('custom.rev', rev)}"
lhash = "{label('custom.hash{phase}',node|short)}"
lage = "{label('custom.age', date|age)}"
ldesc = "{desc}"
luser = "{label('custom.user', author|user)}"
ltag = "{label('custom.tag',tags)}"
